[test-groups]
iree-device = { max-threads = 1 }

[profile.default]
retries = 0
flaky-result = "fail"

[[profile.default.overrides]]
filter = "package(noise-net-iree) and binary(streaming)"
test-group = "iree-device"
slow-timeout = { period = "2m", terminate-after = 2 }
